using System;
using UnityEngine;

namespace Chess.Scripts.Core {
    public  class ChessPlayerPlacementHandler :MonoBehaviour {

        //private void Start() {
        //    ChessPiece piece = GetComponent<ChessPiece>();

        //    if (piece == null)
        //    {
        //        Debug.LogError("ChessPiece component is missing from this GameObject.");
        //        return;
        //    }


        //    transform.position = ChessBoardPlacementHandler.Instance.GetTile(piece.row, piece.column).transform.position;

        //    if (ChessBoardPlacementHandler.Instance == null)
        //    {
        //        Debug.LogError("ChessBoardPlacementHandler.Instance is null. Ensure the ChessBoardPlacementHandler is in the scene and properly initialized.");
        //        return;
        //    }


        //    ChessBoardPlacementHandler.Instance.AddPiece(piece);
        //   Debug.Log($" row ={piece.row}, colomn ={piece.column} is equipped");
        //}
    }
}